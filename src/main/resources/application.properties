
spring.application.name=tax
server.port=9090

#LDAP Configuration
spring.ldap.urls=ldap://host.docker.internal:389
spring.ldap.base=cn=admin,dc=mairie,dc=com
spring.ldap.username=cn=admin,dc=mairie,dc=com
spring.ldap.password=admin

ldap.users.base=ou=users,dc=mairie,dc=com

spring.datasource.url=jdbc:postgresql://localhost:5440/taxCollect
spring.datasource.username=directus
spring.datasource.password=directus
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Flyway configuration
spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-version=1

spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true
#spring.profiles.active=dev

##Keycloak config
spring.security.oauth2.resourceserver.jwt.issuer-uri=http://192.168.1.4:8080/realms/mairie
spring.security.oauth2.resourceserver.jwt.jwk-set-uri=${spring.security.oauth2.resourceserver.jwt.issuer-uri}/protocol/openid-connect/certs

jwt.auth.converter.resource-id=tax-backend
jwt.auth.converter.principal-attribute=preferred_username

## keycloak admin settings
keycloak.admin.host=http://192.168.1.4:8080
keycloak.admin.realm=mairie
keycloak.admin.clientId=keycloak-admin-client
keycloak.admin.clientSecret=NMG9dhW6v454x0mlmw8p7aoppraZYLv1
#keycloak.admin.clientUUID=ef90c853-2e73-4ebe-8d8f-a46aa7211789

#swagger-ui config
springdoc.show-actuator=true
# Disabling the /v3/api-docs endpoint
springdoc.api-docs.enabled=true
# Disabling the swagger-ui
springdoc.swagger-ui.enabled=true