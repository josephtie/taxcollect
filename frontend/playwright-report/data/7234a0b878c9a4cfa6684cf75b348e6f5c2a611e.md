# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: auth.spec.ts >> Authentication >> E2E-AUTH-04: Logout
- Location: tests/e2e/auth.spec.ts:36:3

# Error details

```
Error: expect(page).toHaveURL(expected) failed

Expected pattern: /dashboard/
Received string:  "http://localhost:3000/login"
Timeout: 5000ms

Call log:
  - Expect "toHaveURL" with timeout 5000ms
    11 × locator resolved to <html lang="fr">…</html>
       - unexpected value "http://localhost:3000/login"

```

```yaml
- text: T TaxCollect
- heading "Connexion à votre compte" [level=2]
- paragraph: Tableau de bord de gestion des taxes
- text: Nom d'utilisateur
- textbox "Nom d'utilisateur":
  - /placeholder: admin
  - text: admin@mairie.ci
- text: Mot de passe
- textbox "Mot de passe":
  - /placeholder: ••••••••
  - text: admin
- checkbox "Se souvenir de moi"
- text: Se souvenir de moi
- link "Mot de passe oublié?":
  - /url: "#"
- button "Se connecter"
- img
- heading "Erreur de connexion" [level=3]
- text: NetworkError when attempting to fetch resource. Ou
- button "Admin"
- button "Trésor"
- img
- heading "Identifiants de démonstration" [level=3]
- paragraph:
  - strong: "Admin:"
  - text: admin / password
- paragraph:
  - strong: "Trésor:"
  - text: tresor / password
- paragraph:
  - strong: "Agent:"
  - text: alix / alix123
```

# Test source

```ts
  1  | import { test, expect } from '@playwright/test'
  2  | 
  3  | const ADMIN_EMAIL = 'admin@mairie.ci'
  4  | const ADMIN_PASSWORD = 'admin'
  5  | 
  6  | test.describe('Authentication', () => {
  7  |   test('E2E-AUTH-01: Login admin réussi', async ({ page }) => {
  8  |     await page.goto('/')
  9  |     await expect(page).toHaveURL(/login/)
  10 | 
  11 |     await page.fill('[data-testid="email-input"]', ADMIN_EMAIL)
  12 |     await page.fill('[data-testid="password-input"]', ADMIN_PASSWORD)
  13 |     await page.click('[data-testid="login-button"]')
  14 | 
  15 |     await expect(page).toHaveURL(/dashboard/)
  16 |     await expect(page.locator('h1')).toContainText(/Dashboard/i)
  17 |   })
  18 | 
  19 |   test('E2E-AUTH-02: Login échec — credentials invalides', async ({ page }) => {
  20 |     await page.goto('/')
  21 |     await expect(page).toHaveURL(/login/)
  22 | 
  23 |     await page.fill('[data-testid="email-input"]', 'wrong@email.com')
  24 |     await page.fill('[data-testid="password-input"]', 'wrongpassword')
  25 |     await page.click('[data-testid="login-button"]')
  26 | 
  27 |     await expect(page.locator('[data-testid="error-message"]')).toBeVisible()
  28 |     await expect(page).toHaveURL(/login/)
  29 |   })
  30 | 
  31 |   test('E2E-AUTH-03: Accès sans auth → redirect login', async ({ page }) => {
  32 |     await page.goto('/dashboard')
  33 |     await expect(page).toHaveURL(/login/)
  34 |   })
  35 | 
  36 |   test('E2E-AUTH-04: Logout', async ({ page }) => {
  37 |     // Login first
  38 |     await page.goto('/')
  39 |     await page.fill('[data-testid="email-input"]', ADMIN_EMAIL)
  40 |     await page.fill('[data-testid="password-input"]', ADMIN_PASSWORD)
  41 |     await page.click('[data-testid="login-button"]')
> 42 |     await expect(page).toHaveURL(/dashboard/)
     |                        ^ Error: expect(page).toHaveURL(expected) failed
  43 | 
  44 |     // Logout
  45 |     await page.click('[data-testid="logout-button"]')
  46 |     await expect(page).toHaveURL(/login/)
  47 |   })
  48 | })
  49 | 
```